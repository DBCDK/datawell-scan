{
    "appName": "datawell-scan-service",
    "hazelcast": {
        "clusterName": "datawell-scan-${SCOPE}",
        "cacheConfig": {
            "profileService": {
                "timeout": "${CACHE_TIMEOUT}",
                "timeUnit": "Seconds"
            },
            "profileServiceError": {
                "timeout": "${CACHE_TIMEOUT_ERROR}",
                "timeUnit": "Seconds"
            },
            "*": {
                "timeout": "${CACHE_TIMEOUT}",
                "timeUnit": "Seconds"
            }
        }
    }
}
